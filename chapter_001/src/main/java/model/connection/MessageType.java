package model.connection;

public enum MessageType {
    REQUEST_SUBSCRIBER_ID,
    TEXT_MESSAGE,
    SUBSCRIBER_ID,
    ID_ACCEPTED,
    ID_USED,
    REFUSING,
    USER_INFO,
    JSON,
    DISABLE_USER;
}
